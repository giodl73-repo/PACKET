# Pulse 03: REQUIREMENTS

Settled. Authored `docs/vtrace/REQUIREMENTS.md`: 16 requirements (`REQ-001..016`)
derived from MISSION + CONOPS, each tracing to a parent `NEED-*`/`CON-*`/`OPS-*`;
quality checklist; CONOPS trace review; 5 deferrals (`DEF-001..005`). Role-review
fixed point: added REQ-007 requiring the **load + redundancy basis** (peak vs average;
single vs diverse path) be named — the connectivity analog of PYLON's contingency
basis. REQ-016 makes the **scale tag** a hard requirement (NEED-008).

Validation: `proof check .`, `git diff --check`. Next: SPECIFICATION_BASELINE.
